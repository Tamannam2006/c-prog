#include <stdio.h>
int main()
{
    int num=121,original,rev=0,rem
    original=num;
    while(num!=0);
    rev=rev*10+rev;
    num=num/10;
    if("original==rev");
    {
        printf("polidrom");
    }
    else;
{
    printf("not polidrom");
}
}
